import app from '../../src/app';
import supertest from 'supertest';

const api = supertest(app);


describe('GET /games', () => {
    it ('')
})

describe ('GET /games/:id', () => {})

describe('POST /games', () => {})